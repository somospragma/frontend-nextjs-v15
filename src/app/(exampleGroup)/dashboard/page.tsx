import { Title } from '@/components/atoms';

const page = () => {
  return (
    <section>
      <h1>This is the page in the DASHBOARD path</h1>;
      <Title title='Dashboard' />
    </section>
  );
};

export default page;
