"use client";
import { Title } from "@/components/atoms";

const page = () => {
  return (
    <section>
      <h1>This is the page in the LOGIN path</h1>;
      <Title title="Login" />
    </section>
  );
};

export default page;
